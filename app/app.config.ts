export default defineAppConfig({
  ui: {
    colors: {
      primary: 'brand',
      neutral: 'slate'
    },
    toaster: {
      defaultVariants: {
        position: 'top-right'
      }
    }
  }
})
